# {{ foundry.name }} — static site

One HTML file, no build step, no dependencies. Serve it from any object
bucket, CDN, or `python3 -m http.server`. Deploy with the
`storage/object-store` module plus your CDN of choice, or GitHub Pages.
